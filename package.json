{
  "scripts": {
    "dev": "concurrently \"npm start --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
